package in.timmauld.finance.taqimport.main;

import junit.framework.Assert;

import org.apache.hadoop.conf.Configuration;
import org.apache.hadoop.fs.FileSystem;
import org.apache.hadoop.fs.Path;
import org.junit.Test;

public class TaqAggregatorJobTest {
	@Test 
	public void test() throws Exception { 
//		Configuration conf = new Configuration(); 
//		conf.addResource(new Path("conf/hadoop-local.xml"));
//		Path input = new Path("testinput"); 
//		Path output = new Path("temp/results"); 
//		FileSystem fs = FileSystem.getLocal( conf); 
//		fs.delete( output, true); // delete old output 
//		TaqAggregatorJob driver = new TaqAggregatorJob(); 
//		driver.setConf( conf); 
//		int exitCode = driver.run( new String[] { input.toString() }); 
//		Assert.assertEquals(exitCode, 0); 
	}
 
}
